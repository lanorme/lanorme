# The SECRETPY-001 test corpus intentionally contains fake, secret-shaped
# strings (example AWS / Stripe / Slack / GitHub keys) so the detector has
# positives to catch. They are not real credentials. Keep them out of the
# secret-scanning alerts so the security tab is not noisy.
paths-ignore:
  - "tests/fixtures/security_hardcoded_secrets/**"
