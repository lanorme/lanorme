"""Code under a Usage or Example header illustrates a call; it does not disable one."""

from __future__ import annotations


def register(name: str, **opts) -> None:
    # Example:
    #     register("svc", timeout=30)
    #     client.get(url)
    pass


# e.g.:
#     result = compute(x)
VALUE = 1
