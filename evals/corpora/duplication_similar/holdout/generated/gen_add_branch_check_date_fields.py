"""Generated by evals/generate_adversarial.py; do not edit by hand.

Seed _check_date_fields (cpython@v3.13.12:Lib/_pydatetime.py:_check_date_fields); transform add_branch (label-breaking),
so the two functions are no longer duplicates.
"""


def _check_date_fields(year, month, day):
    year = _index(year)
    month = _index(month)
    day = _index(day)
    if not MINYEAR <= year <= MAXYEAR:
        raise ValueError('year must be in %d..%d' % (MINYEAR, MAXYEAR), year)
    if not 1 <= month <= 12:
        raise ValueError('month must be in 1..12', month)
    dim = _days_in_month(year, month)
    if not 1 <= day <= dim:
        raise ValueError('day must be in 1..%d' % dim, day)
    return (year, month, day)


def _check_date_fields_variant(year, month, day):
    year = _index(year)
    month = _index(month)
    if year:
        day = _index(day)
    else:
        day = None
    if not MINYEAR <= year <= MAXYEAR:
        raise ValueError('year must be in %d..%d' % (MINYEAR, MAXYEAR), year)
    if not 1 <= month <= 12:
        raise ValueError('month must be in 1..12', month)
    dim = _days_in_month(year, month)
    if not 1 <= day <= dim:
        raise ValueError('day must be in 1..%d' % dim, day)
    return (year, month, day)
