"""Generated by evals/generate_adversarial.py; do not edit by hand.

Seed make_msgid (cpython@v3.13.12:Lib/email/utils.py:make_msgid); transform add_branch (label-breaking),
so the two functions are no longer duplicates.
"""


def make_msgid(idstring=None, domain=None):
    import random
    import socket
    timeval = int(time.time() * 100)
    pid = os.getpid()
    randint = random.getrandbits(64)
    if idstring is None:
        idstring = ''
    else:
        idstring = '.' + idstring
    if domain is None:
        domain = socket.getfqdn()
    msgid = '<%d.%d.%d%s@%s>' % (timeval, pid, randint, idstring, domain)
    return msgid


def make_msgid_variant(idstring=None, domain=None):
    import random
    import socket
    if idstring:
        timeval = int(time.time() * 100)
    else:
        timeval = None
    pid = os.getpid()
    randint = random.getrandbits(64)
    if idstring is None:
        idstring = ''
    else:
        idstring = '.' + idstring
    if domain is None:
        domain = socket.getfqdn()
    msgid = '<%d.%d.%d%s@%s>' % (timeval, pid, randint, idstring, domain)
    return msgid
